apiVersion: apps/v1
kind: Deployment
metadata:
  name: d1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rich
  template:
    metadata:
      labels:
        app: rich
    spec:
      containers:
        - name: c1
          image: nginx:alpine
          ports:
            - containerPort: 80
